LONGFORM V8 — title-only cards + full-height composer

Upload Longform.html together with the current shared Script.js and Style.css from this package.

What changed:
- Collapsed Saved Thoughts show the saved title only; the old first-line body preview is hidden.
- The title field now has its own composer grid row, so the body editor fills the remaining writing panel again instead of starting small and growing.
- Existing title storage and Safe Sync V2 behavior are unchanged.
- Existing card collapse/tools/image behavior remains intact.

FOLLOW-UP
- Saved thoughts now have a Pin to Top action in Tools.
- Pinned cards stay above unpinned cards without changing the normal order of everything else.
